load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "fimage_crop_benchmark",
    srcs = ["fimage_crop_benchmark.cpp"],
    copts = [
        "-std=c++20",
        "-O3",
    ],
    deps = [
        "//:fimage",
        "@fastslide//:formats_aperio",
        "@fastslide//:formats_mrxs",
        "@fastslide//:formats_qptiff",
        "@google_benchmark//:benchmark",
    ],
)

cc_binary(
    name = "fimage_resize_benchmark",
    srcs = ["fimage_resize_benchmark.cpp"],
    copts = [
        "-std=c++20",
        "-O3",
    ],
    deps = [
        "//:fimage",
        "@google_benchmark//:benchmark",
    ],
)
